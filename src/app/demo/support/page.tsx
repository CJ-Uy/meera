import { StudentSupportChat } from "@/features/meera-support/student-support-chat";

export const metadata = {
	title: "Meera — Live Support",
};

export default function SupportDemoPage() {
	return <StudentSupportChat />;
}
